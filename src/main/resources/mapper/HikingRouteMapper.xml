<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kopo.kpaas.mapper.HikingRouteMapper">

    <!-- Insert new hiking route -->
    <insert id="insertHikingRoute">
        INSERT INTO HikingRoutes (route_id, route_geometry, sec_len, up_min, down_min, cat_nam, mntn_nm, ag_geom)
        VALUES (#{routeId}, #{routeGeometry}, #{secLen}, #{upMin}, #{downMin}, #{catNam}, #{mntnNm}, #{agGeom})
    </insert>

    <!-- Retrieve all saved hiking routes -->
    <select id="findAllRoutes" resultType="kopo.kpaas.dto.HikingRouteDTO">
        SELECT * FROM HikingRoutes
    </select>

    <select id="getPolygonPoints" resultType="kopo.kpaas.dto.PolygonPointsDTO">
        SELECT
            FirstPointLat, FirstPointLng,
            SecondPointLat, SecondPointLng,
            ThirdPointLat, ThirdPointLng,
            FourthPointLat, FourthPointLng,
            CentroidLat, CentroidLng
        FROM PolygonPoints
                 LIMIT 1
    </select>
</mapper>
